# Two digit sum

two_digit_str = str(input('Input digit ? '))

first_digit = int(two_digit_str[0])
second_digit = int(two_digit_str[1])

sum_of_digits = first_digit + second_digit

print(f'{first_digit} + {second_digit} = {sum_of_digits}')